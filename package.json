{
  "name": "conciencia-academica",
  "private": true,
  "description": "Raíz del repo: scripts para levantar back + front juntos",
  "scripts": {
    "dev": "concurrently -k -n back,front -c cyan,magenta \"npm run dev:back\" \"npm run dev:front\"",
    "dev:back": "cross-env PORT=3000 npm run dev --prefix back",
    "dev:front": "npm run dev --prefix front"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "cross-env": "^7.0.3"
  }
}
